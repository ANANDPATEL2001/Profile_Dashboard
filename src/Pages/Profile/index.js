// Import necessary React and Bootstrap components
import React from 'react';
import ProfileContainer from "../../ui-components/ProfileContainer"

// Functional component for the user profile page
const UserProfilePage = () => {
    return (
        <ProfileContainer />
    );
};

export default UserProfilePage;
